# RAG

M9 uses a `KnowledgeRetriever` port with PostgreSQL/pgvector implementation, organization/scope filtering, versioned ingestion, source references, and injection defenses. Tenant clinical data is never shared by default.

See REQ-RAG-* and ADR-007.
