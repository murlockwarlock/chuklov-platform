# AI

Laravel AI SDK is the current provider-independent runtime. Organization and agent configuration select provider/model/prompt/KB scopes. AI runs are versioned/auditable and outputs remain drafts until specialist review where clinical.

M0 only proves SDK fakes; no external call occurs in CI. See REQ-AI-* and ADR-008.
