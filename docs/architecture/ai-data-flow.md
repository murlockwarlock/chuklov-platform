# AI Data Flow

Permitted record references → organization/scope authorization → versioned prompt/tools → provider adapter → structured output → run registry → review state/delivery. Queue payloads carry IDs, not medical free text. Retrieved documents are untrusted input.
