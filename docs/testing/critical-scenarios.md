# Critical Scenarios

M0: organization override is ignored; cross-org Service hidden; CRM auth required; Service action feeds Portal; pgvector exists; Redis queue processes safe ID; AI fake makes no external call.

Later milestone critical scenarios are defined by relevant REQ acceptance criteria and the master plan. Add them when implementation begins rather than creating empty tests.
